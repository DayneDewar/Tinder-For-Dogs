<div class="container">
    <div class="row">
        <% @matches.each do |match|%>
            <div class="col-sm-4">
                <div class="match-title rounded" style="background-image: url('<%= url_for match.images.first %>')">
            <%# <%= image_tag match.images.first, class "img-fluid"%>
                </div>
            </div>
        <% end %>
    </div>
</div>